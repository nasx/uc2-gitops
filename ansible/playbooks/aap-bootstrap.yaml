- name: Ansible Automation Platform Bootstrap and Configuration
  become: false
  gather_facts: false
  hosts: localhost
  roles:
    - role: aap-setup-auth
    - role: aap-bootstrap
    - role: aap-config
